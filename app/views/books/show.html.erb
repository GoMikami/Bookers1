<body>
  
  <% if flash[:notice] %>
     	<p><%= flash[:notice] %></p>
    <% end %>
    
  <p>
    <strong>Title:</strong>
    <%= @book.title %>
  
    <strong>Body:</strong>
    <%= @book.body %>
  </p>
  <%= link_to "Show",book_path(@book) %>
  |
	<%= link_to "Back", books_path(@books) %>

</body>
